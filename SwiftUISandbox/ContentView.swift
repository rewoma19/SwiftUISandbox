import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("Main view")
                    .font(.title)
            }
        }
    }
}

#Preview {
    ContentView()
}
